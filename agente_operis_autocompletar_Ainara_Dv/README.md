# agente_operis_autocompletar_Ainara_Dv

Agente en desarrollo local, todavía no subido a este repositorio.

El código y la documentación completa viven en la carpeta de trabajo local
`Agente_Operis/`, en tres copias:

- `Agente_Operis/agente_operis/` — copia de trabajo principal (motor
  `reglas`, gratis y determinista, por defecto).
- `Agente_Operis/Demo_sin_IA/` — misma configuración, es el contenido que
  estuvo preparado en esta carpeta del repo antes de moverse de vuelta
  aquí en local.
- `Agente_Operis/agente_operis_llm/` — motor `llm` (Groq) por defecto,
  para probar la extracción con IA real.

Cuando se confirme qué versión subir, su contenido se copiará aquí y se
hará el commit correspondiente.
